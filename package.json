{
  "name": "spapi",
  "version": "1.0.0",
  "description": "# Setup `npm install` `npm start`",
  "main": "app.js",
  "scripts": {
    "start": "nodemon app.js",
    "test": "NODE_ENV=test mocha --watch ./tests/**/*.test.js",
    "test:ci": "NODE_ENV=test mocha ./tests/**/*.test.js"
  },
  "author": "LeonardoTeam",
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^7.2.0",
    "nodemon": "^2.0.4",
    "supertest": "^3.3.0"
  },
  "dependencies": {
    "body-parser": "^1.20.1",
    "express": "^4.18.2",
    "mongodb": "^3.5.8",
    "superagent": "^4.0.0-beta.5"
  },
  "directories": {
    "test": "tests"
  }
}
